# ── Core agent ──────────────────────────────────
moshi                        # PersonaPlex / full-duplex voice model
websockets             # WebSocket client for PersonaPlex server
sounddevice         # Microphone input / speaker output

# ── Web server (backend ↔ frontend) ─────────────
fastapi           # ASGI web framework with WebSocket support
uvicorn[standard]    # ASGI server to run FastAPI

# ── AI / LLM ────────────────────────────────────
google-generativeai  # Gemini API — booking extraction + chat
anthropic           # Claude API (optional / legacy)

# ── RAG ─────────────────────────────────────────
chromadb              # Vector store (persisted, local)
sentence-transformers # Local embedding model (all-MiniLM-L6-v2)

# ── Document loaders ────────────────────────────
pypdf              # Read .pdf files
python-docx          # Read .docx files

# ── Data / utils ────────────────────────────────
numpy               # Audio array processing
python-dotenv